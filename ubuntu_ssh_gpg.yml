- name: Ubuntu SSH and GPG/Pass Setup
  hosts: localhost
  become: true
  vars:
    username: galib
    source_key: "./ssh/id_rsa"
    dest_key: "/home/{{ username }}/.ssh/id_rsa"

  tasks:
    - include_tasks: tasks/ubuntu/ssh.yml
    - include_tasks: tasks/ubuntu/gpg_pass.yml
