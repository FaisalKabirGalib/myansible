- name: Install nvim dependencies via Homebrew
  community.general.homebrew:
    name:
      - node
      - neovim
      - luarocks
      - ripgrep
      - lazygit
    state: present
  tags:
    - nvim
    - install
  ignore_errors: true
