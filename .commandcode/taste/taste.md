# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# just
- Use `just` as a command runner instead of ad-hoc shell scripts for project tasks like `build_docker`, `setup_key`, etc. Confidence: 0.65

